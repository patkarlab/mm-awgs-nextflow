# HANDOFF — 2026-09-16 (full day) → next session

Repo: `patkarlab/mm-awgs-nextflow`, `main` at `9844a71`, pushed. Working tree
clean apart from the samplesheets (gitignored). Session id for resume is
unchanged: `b079f29f-e83b-447e-b55d-c8836e10c798`.

## State of the cohort

- **Current results: `results_v7_14`** (14 samples; `samplesheets/samplesheet_v7_14.csv`).
  Bundle `report_results_v7_14_nobam.zip` (619 MB) in the repo root. Every
  required artefact present for all 14 (`bin/check_published_outputs.sh`).
- **`results_v7`** (15 samples) is the archive of the pre-exclusion cohort.
  Keep until the 14-sample bundle has been read end to end, then delete.
- **12F2023670 is out of the cohort.** `bin/mmfingerprint.py` on its on-target
  common SNPs: no heterozygous peak at 0.5 (flat allele-fraction histogram),
  chrX 0.60 and chrY 0.10 of autosomal depth, a third of the usable sites of a
  clean sample. A two-person mixture, one male; the second genome is female
  and not on its flowcell (its co-barcoded sample 11F202611829 is male and
  none of its junctions appear in 12F2023670), so the mixing predates
  barcoding. Evidence in `docs/qc/12F2023670.fingerprint.*`. Wet-lab trace of
  the 20 July sort/extraction is a lab question, not a pipeline one. Its
  hyperdiploid call was the artefact predicted on 15 Sep.

## What changed today (all pushed)

| Commit | Change |
|---|---|
| `a8cd13e` | Report bundle waits for publishDir and fails on gaps (`bundle_guard_v1`); `check_published_outputs.sh`; v6 filter writes a header on an empty table |
| `56eae7f` | `ICHORCNA_PLOT` on the hg38 track replaces mmkaryo/mmbaf/COPY_NUMBER; ISCN lists abnormal arms only; legible grids |
| `c9baef7` | genebaf: BAF at the eight wide windows only (`assets/aWGS_PCN_v7_baf_regions_hg38.bed`), Ig excluded; covered span on arm verdicts |
| `5477a19` | 15 Sep handoff filed in docs/; superseded copy-number patch scripts removed |
| `777922f` | envs: GENEBAF and ICHORKARYO run in awgs_sv (they had no selector) |
| `41c18b9`, `ee9ed9e` | nanomonsv wired as a confirmation column (`support_nanomonsv`, `filter_nanomonsv`, `nanomonsv_confirmed`); HPRC CHM13 control panel; prefix is inside its directory |
| `bc3d2da` | Breakend mates collapsed before SURVIVOR (`collapse_bnd_mates.py`); `check_caller_retention.sh` — Severus retention identical on all 15 |
| `62251b0` | `mmfingerprint.py`; 12F2023670 evidence |
| `b951333` | Dashboard revision (see below); `merge_translocations.py` band/ig_region orientation fix |
| `d15d62b` | genebaf writes every usable site to `sites.tsv` with a `call` column |
| `9844a71` | Only EMBED_REPORT_ASSETS publishes the bundle (DASHBOARD's publish was racing it) |

## Dashboard (dashboard_mm_v3)

- Cohort index: purity, depth, ploidy class + trisomies, defining translocation
  with nanomonsv confirmation, MYC, confirmed loci, 17p/1p/1q/13q arm events
  with cell fraction, clinical SNV count, identity flag.
- Rearrangements tab: reads `translocations.tsv`; three levels — event group
  (anchor) → partner locus (both breakends within 1 Mb; `locus_tol` in
  `parsers/rearrangements.py`) → breakends. Reportable table, then Other
  junctions. Reading key and column key. Filters. Click a row for details.
- Copy number & allelic state tab (one tab; BAF/LOH and ichorCNA tabs
  merged in): per-chromosome window table with on-target depth log2,
  ichorCNA CN/event/cell fraction, allelic state, hets, reading; click an
  assessed window for its depth + allele-fraction plot (inline SVG from
  genebaf bins and sites); arm-test line; cytobands and ichorCNA fit collapsed.
  Targets track labelled with window names.
- Sidebar sticks. Delivered by `bin/apply_dashboard_mm.py` +
  `bin/dashboard_mm_v3_files.tar.gz`; both committed.

## Findings worth remembering

- **Publish races are real.** Two independent ones today: bundle scanning the
  tree before publishDir finished (fixed with the guard), and two stages
  publishing the same directory (fixed by publishing once). Anything that
  reads the published tree from inside the pipeline needs ordering on the
  files, not on task completion.
- **nanomonsv works as a confirmation layer.** 0–4 confirmed loci per sample;
  on 11F202615394 the one confirmed junction is the FISH t(11;14). Two-read
  junctions are "not reported", never "rejected".
- **Mate collapse changed nothing downstream** (junction counts identical,
  Severus retention identical), which means the positional pass was already
  repairing every split; the collapse fixes SUPP_VEC at the source.
- **The TP53 window is right-sized.** 13/15 samples carry 170–414 hets in it;
  11F202610129 is depth-limited (7.9×, 3 hets), 12F20264455 has 15 hets
  because 17p is deleted at 93% CF, which the window read correctly.
- **11F202617209** (FISH normal on 200 cells): 1q ×4, 6p/8q/15q ×3, 10p loss
  on the copy-number plot. Not in conflict with centromere-probe FISH, but
  not "nothing". Worth a 1q21 probe if the marrow is available.
- **11F20265533**: 22 somatic candidates, none kept in-panel; check whether
  it is a candidate-count outlier in the cohort `filter_summary.tsv`.
- **Purity of 11F202611829** is 1.00 on the sample sheet, 0.90 in the 15 Sep
  handoff. Check the sheet.
- **ichorCNA runs on autosomes only** (`--chrs c(1:22)`), so the sex field in
  ichorkaryo JSON is the sample sheet's, never checked against depth.
  `mmfingerprint.py` is the check.

## Open items

1. **Off-target arm LOH** (agreed in principle): per-arm two-allele fraction
   at common SNPs from the off-target reads at a few-fold depth, calibrated
   against the sample's own diploid arms; gives 17p, 13q, 1p, 16q, 20q, 6p a
   real LOH verdict. genebaf extension + re-run. Not started.
2. Whether a ≥1 Mb assessed window may carry its own LOH verdict in
   `classify()` (today the window row is the verdict; the arm line says so).
3. `--scStates c(1,3)` experiment for the subclonal trisomies (11F202611457,
   11F20265533) — standalone test, not wired.
4. hg38 gene model for the chromosome pages (`bin/build_mm_gene_model.py`
   from the hg38 RefSeq GFF; `params.gene_model_hg38`).
5. chrX/chrY in ichorCNA (`--chrs "c(1:22,'X','Y')"`, chrTrain unchanged) —
   only when the cohort is otherwise frozen.
6. `results_v7` deletion once the 14-sample bundle has been read.
7. The 15 Sep dashboard leftover: `bin/dashboard_builder/templates/{translocations_tab,baf_loh_tab,ichor_tab}.html.j2`
   are no longer included; delete after one release.
8. The Nextflow cache directories the 15 Sep handoff called corrupt did not
   match any id under `.nextflow/cache`; nothing was removed. Live session
   intact.

## Conventions confirmed today

- One step per message when driving gandalf; wait for the paste.
- Files from Claude arrive via `~/inbox/from_claude/` and are `cp`'d into
  place; files to Claude go through `~/inbox/to_claude/` and are attached.
- Patch scripts: string-anchored, `.bak`, sentinel, `--check`, `--revert`.
  Whole-file replacement (`apply_dashboard_mm.py`) only when several files
  are rewritten, with a hash manifest of the allowed starting versions.
- Every pipeline change verified here first: stub DAG on the current tree,
  process bodies executed on synthetic data, dashboard built on the real
  subset bundle and screenshotted in a headless browser.
